# simulator — STUDENT DELIVERABLE (Phase 3)

Synthetic traffic via the public API, funnel-consistent:
browse → (80%) preview → (60%) add_to_cart → (70%) checkout, weighted by the
SAME Zipf popularity as the seeded catalog so leaderboards and funnels show
recognizable patterns. Opt-in Compose profile `traffic`; ORDERS_PER_MINUTE
env. Landmine #8: several evals give false failures while it runs.
